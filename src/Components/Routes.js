import { Routes, Route } from 'react-router-dom';


function Routes(props) {
    return (
       <Routes>
        <Route path = "/">
            <Route index element = {} />
        </Route>
       </Routes>
    );
}

export default Routes;